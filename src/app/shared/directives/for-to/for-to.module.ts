import { NgModule } from '@angular/core';
import { ForToDirective } from './for-to.directive';

@NgModule({
  declarations: [ForToDirective],
  exports: [ForToDirective],
})
export class ForToModule {}
