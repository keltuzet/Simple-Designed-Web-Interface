import { ForToDirective } from './for-to.directive';

describe('ForToDirective', () => {
  it('should create an instance', () => {
    const directive = new ForToDirective();
    expect(directive).toBeTruthy();
  });
});
