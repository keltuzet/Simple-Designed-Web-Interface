export enum WeatherUnits {
  Temperature = 'temp',
  Percent = 'percent',
  Length = 'length',
  ShortLength = 'length_short',
  Speed = 'speed',
  Pressure = 'pressure',
}

export enum TemperatureUnits {}
